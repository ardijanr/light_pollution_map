use std::process::Command;
use std::fmt::format;
use std::fs;


const DATA_DIR: &str = "";

fn main(){

}


fn convert_downloaded_data()->Option<bool>{
    let paths = fs::read_dir("./").unwrap();

    return Some(true);
}


